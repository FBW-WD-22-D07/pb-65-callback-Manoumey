{
  "scripts": {
    "test": "jest",
    "preinstall": "npm_config_yes=true npx github:DCI-EdTech/autograding-setup --no"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "rewire": "^5.0.0"
  },
  "jest": {
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}